# Cargo build settings for Picroom

[build]
# Use a single shared target dir to reduce disk usage
target-dir = "target"

[net]
# Allow offline rebuilds (CI uses sccache)
git-fetch-with-cli = true

[registries.crates-io]
# Default registry (no overrides)